def name_standrad(L):
	return map(str.capitalize,L)
#	def standrad(name):
#		return name[0].upper()+name[1:len(name)].lower();
#	return map(standrad, L)
L = ['adam', 'LISA', 'barT']
print name_standrad(L)
	